# Exact pins for reproducible builds. CI and the Railway deploy install from this
# file with no lockfile, so an unpinned `>=` could silently resolve to an
# untested newer major on a redeploy weeks later and break prod with no code
# change. Bump these deliberately (and re-run the test suite) rather than letting
# them float. The genblaze-* trio must stay on the same 0.3.x line
# (genblaze-nvidia requires genblaze-core within that range).
fastapi==0.139.2
uvicorn[standard]==0.51.0
python-multipart==0.0.32
python-dotenv==1.2.2
pydantic==2.13.4
pydantic-settings==2.14.2
boto3==1.43.52
Pillow==12.3.0
pypdf==6.14.2
httpx==0.28.1
stripe==12.5.1
genblaze-core==0.3.6
genblaze-nvidia==0.3.1
genblaze-s3==0.3.5

# Dev / quality
ruff==0.15.22
pytest==9.1.1
pytest-asyncio==1.4.0
